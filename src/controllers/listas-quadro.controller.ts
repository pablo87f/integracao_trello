import _ from "lodash";
import express from 'express';

namespace ListaQuadroController {
    
    export function index(req: express.Request, res: express.Response) {
            
    }
}

export default ListaQuadroController